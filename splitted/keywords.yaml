get:
  description: "get all keywords"
  tags:
    - stories
  parameters: []
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "components.yaml#/Keyword"
